**HOW TO INSTALL IT**

1. Enter the program directory
2. Run `make install`
3. Go to `http://localhost:8100`
4. To stop, run `docker-compose down -v`

**HOW TO RUN IT**

1. Enter the program directory
2. Run `make run`
3. Go to `http://localhost:8100`
4. To stop, run `docker-compose down -v`
